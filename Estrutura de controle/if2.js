function teste(num){
    if (num > 7)    // nesse caso se não for usado as chaves para definir o bloco, o programa irá só executar uma sentença.
        console.log(num)

        console.log("final") // nesse caso sempre vai ser executador independente do resultado do filtro, por está fora da estrutura condicional.
} 
teste(8)
teste(6)